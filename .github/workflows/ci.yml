name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  dotnet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 10.0.x
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Build
        run: dotnet build DataProcessingApp.sln
      - name: Test
        run: dotnet test tests/DataProcessingApp.Tests --no-build --verbosity normal
      - name: Format check
        run: dotnet format DataProcessingApp.sln --verify-no-changes
      - name: Generate XML files from JSON
        run: python src/PythonDataApp/JsonToXml.py
      - name: Load smoke test
        run: dotnet run --project src/DataProcessingApp.ConsoleApp -- load
        env:
          DPA_BaseDataDir: JSONFiles
          DPA_XmlDataDir: XMLFiles

  python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          pip install -r src/PythonDataApp/requirements.txt -r tests/python/requirements.txt
      - name: Test
        run: python -m pytest tests/python -q

  database:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 10.0.x
      - name: Build
        run: dotnet build DataProcessingApp.sln
      - name: Start SQL Server (docker compose)
        run: docker compose up --detach --wait sqlserver
      - name: Run database integration tests
        run: dotnet test tests/DataProcessingApp.Tests --no-build --filter Category=Integration
        env:
          DPA_TEST_CONNECTION_STRING: "Server=localhost,1433;Database=DataProcessingAppDB;User Id=sa;Password=YourStrong!Passw0rd;TrustServerCertificate=True;Encrypt=True"

  data-drift:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install -r src/PythonDataApp/requirements.txt
      - name: Re-extract 90CM PDFs
        run: python src/PythonDataApp/Extract90CMFromPdf.py
      - name: Rerun converters
        run: |
          python src/PythonDataApp/Process90CMTables.py
          python src/PythonDataApp/ConvertRootTablesToJson.py
          python src/PythonDataApp/Convert2000CMToJson.py
          python src/PythonDataApp/Convert2010CMToJson.py
          python src/PythonDataApp/JsonToXml.py
      - name: Verify source manifest
        run: python src/PythonDataApp/VerifyManifest.py
      - name: Fail on drift from committed JSONFiles and extracted CSVs
        run: |
          git diff --exit-code -- JSONFiles src/PythonDataApp/DataFiles
          test -z "$(git status --porcelain JSONFiles src/PythonDataApp/DataFiles)" || {
            echo "::error::Untracked generated files in JSONFiles or extracted CSVs:"
            git status --porcelain JSONFiles src/PythonDataApp/DataFiles
            exit 1
          }
